# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running SIBS Code (for example via API key or ChatGPT auth supported by Codex).
- SIBS Code starts the server via `codex app-server` per session.
